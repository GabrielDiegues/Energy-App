import { Text, View } from 'react-native';
import styles from '../Styles/GlobalStyles';

const Damages = () => {
    return (
        <View>
            <Text style={styles.title}>Select the affected event:</Text>
        </View>
    );
};

export default Damages;

import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    input: {
        fontSize: 13,
        borderWidth: 1,
        marginTop: 35,
    },
    title: {
        fontSize: 20,
        marginTop: 10,
        marginLeft:10,
        fontWeight: 'bold',
    },
});

export default styles;

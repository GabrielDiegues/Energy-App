import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    input: {
        fontSize: 13,
        borderWidth: 1,
        marginTop: 35,
    },
    title: {
        fontSize: 20,
        marginTop: 10,
        marginLeft:10,
        fontWeight: 'bold',
    },

    buttonContainer: {
        alignItems: 'center',
        borderWidth: 2,
        borderRadius: 8,
        padding: 10,
        marginTop: 30,
        marginBottom: 50,
        marginLeft: 50,
        marginRight: 50,
    },
});

export default styles;
